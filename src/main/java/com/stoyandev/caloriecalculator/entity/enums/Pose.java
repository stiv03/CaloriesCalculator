package com.stoyandev.caloriecalculator.entity.enums;

/**
 * Which angle a progress photo was taken from. Stored as a string so the
 * gallery can group photos into Front / Side / Back columns.
 */
public enum Pose {
    FRONT,
    SIDE,
    BACK
}